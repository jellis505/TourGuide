Our procedure for building the data structures used for object classification can be broken down into 4 stages: image preprocessing, feature extraction, codebook generation, and k-d tree construction.